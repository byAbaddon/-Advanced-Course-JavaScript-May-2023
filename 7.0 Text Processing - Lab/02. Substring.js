(str,s, e) => str.slice(s, s + e)

//--------------------------------(2)------------------

function substring(text, startIndex, endIndex) {
  return text.substring(startIndex, startIndex + endIndex)
}

// console.log(substring("ASentance", 1, 8))
