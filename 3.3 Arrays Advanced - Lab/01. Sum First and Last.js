arr => +arr[0] + +arr.pop()

//--------------------------------(2)--------------------

function sumFirstLast(arr) {
  return  +arr.shift() + +arr.pop()
}

//console.log(sumFirstLast(['20', '30', '40']))
